bcr_test_module:
  module_path: "e2e/bcr"
  matrix:
    platform: ["debian11", "ubuntu2404", "macos"]
    bazel: [7.x, 8.x]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
